image: openjdk:8-jdk-alpine

stages:
  - test

variables:
  PYTHONPATH: $PYTHONPATH:$(pwd)/src

test:
  stage: test
  script:
    - apk add --no-cache python3
    - javac HelloWorld.java
    - python3 src/scenarios/tools/CheckStyle.py
